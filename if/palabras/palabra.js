const texto = "ADSO es una empresa que busca desarrolladores talentosos.";

if (texto.includes("ADSO") && texto.includes("desarrolladores")) {
  console.log("La cadena de texto contiene las palabras ADSO y desarrolladores.");
} else {
  console.log("La cadena de texto no contiene las palabras ADSO y desarrolladores.");
}
